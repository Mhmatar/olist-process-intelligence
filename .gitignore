node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.env*
!.env.example
next-env.d.ts
.vercel
*.tsbuildinfo

# NOTE: data/olist_cases_clean.csv (16 MB) is deliberately NOT ignored — the
# build reads it to compute every aggregate, so ignoring it breaks a fresh
# clone. CLAUDE.md flags the track-vs-LFS call as an open decision; if you move
# it to Git LFS or a release asset, add the ignore rule here at the same time.
